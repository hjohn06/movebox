// Runtime config — values are injected at deploy time via GitHub Actions.
// For local development, set values here directly (do not commit real secrets).
window.MOVEBOX_AI_TOKEN   = window.MOVEBOX_AI_TOKEN   || '';
window.MOVEBOX_AI_PROXY   = window.MOVEBOX_AI_PROXY   || '';
window.GOOGLE_CLIENT_ID   = window.GOOGLE_CLIENT_ID   || '';
