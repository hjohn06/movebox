name = "movebox-ai-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Worker runs on Cloudflare's free tier
# Free tier: 100,000 requests/day — more than enough for personal use

# Secrets (set via CLI, never commit values here):
#   wrangler secret put ANTHROPIC_API_KEY
#   wrangler secret put MOVEBOX_SECRET_TOKEN
